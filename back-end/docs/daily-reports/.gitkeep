# Daily learning reports

This directory holds one MD file per market day, auto-generated by
`app.services.daily_learning` at 17:30 ET Mon-Fri.

Filenames: `YYYY-MM-DD.md` (ET calendar date).

Files are written atomically (tmp + rename) and committed to git so the
history is searchable and reviewable across branches.
